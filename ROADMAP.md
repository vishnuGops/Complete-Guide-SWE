# ROADMAP — DevProMax

DevProMax is a local-first, LeetCode-style training environment for DSA interview prep in **Python** and **Java**, with an on-demand LLM coach (AI Help) that reviews the code written so far until the solution is interview-perfect.

Status legend: `Not started` · `In progress` · `Done` · `Blocked`

Rule: the task table is **always sorted by the Priority column** (1 = do first). When adding a task, insert it at its priority and renumber below it. When a task changes state, update its Status cell in the same commit as the work.

Revision history: section 7, newest last.

---

## 1. Goals

| Goal | What "done" looks like |
| --- | --- |
| Full DSA coverage | Every topic in the 14-topic curriculum has Easy, Medium and Hard problems, ordered as a learning path. |
| LeetCode-grade workspace | Statement + examples + constraints, Monaco editor, Run (sample + custom tests) and Submit (hidden tests), verdicts with diffs, submission history, editorial, hints, notes, timer. |
| LLM coach, not an answer key | With a user-supplied API key (Anthropic or Gemini), an **AI Help** button gives constructive, rubric-based feedback and hints on the code written so far. The coach never dumps the full solution unless the problem is already solved and the user asks. |
| Visible progress | Scrollable problem list shows Not started / In progress / Solved / Mastered; filterable by topic, difficulty and status; a dashboard shows strengths and weak spots and exports a shareable skills report. |
| Not AI slop | A deliberate design system (tokens, one accent, dense tool-like layout, real keyboard support, dark/light) with a written set of principles that reviews are checked against. |
| Trustworthy | Unit, contract, integration (real Python/Java processes), component and end-to-end tests run in CI. Every problem's reference solutions must pass its own tests in both languages before it can merge. |

Out of scope for v1: accounts and cloud sync, multi-tenant hosting, languages beyond Python and Java, contests, discussion forums, acceptance rates and runtime percentiles (meaningless for one user), company tags, LSP-grade autocomplete, code formatting on save.

---

## 2. Architecture decisions (with reasoning)

| # | Decision | Reasoning | Alternatives rejected |
| --- | --- | --- | --- |
| D1 | **Local-first single-user app.** `npm start` launches a server on localhost and opens the browser. Data lives in a local SQLite file. | No infra to run, works offline except LLM calls, the API key never leaves the machine, and the target user is one developer practising at their desk. | Hosted multi-user SaaS (auth, billing, abuse handling of a code runner — none of it needed for the stated goal). |
| D2 | **TypeScript end-to-end, three-workspace npm monorepo.** `apps/web` (React 19 + Vite + React Router), `apps/server` (Fastify; judge and problem loader live inside it as folders), `packages/shared` (types + zod schemas). | One toolchain, one test runner (Vitest), API types shared with the UI so the contract cannot drift, Node 24 already installed. Running Python/Java is a subprocess spawn whichever language the backend uses, so backend choice does not affect judge quality; Node's `child_process` gives streaming, timeouts and process-tree control. Problem content is data, so Python/Java contributors never need to touch the backend. Confirmed by owner 2026-09-16; collapsed from five workspaces to three in the audit to cut build-graph overhead. | Python FastAPI backend (second toolchain, two test runners, duplicated types). Electron (heavier, no benefit over a localhost tab). Separate `judge`/`problems` packages (extra manifests, no reuse yet). |
| D3 | **Harness-based judge running local subprocesses** (owner confirmed local-only, 2026-09-16). User submits a `Solution` class. A per-language harness deserialises JSON test inputs, invokes the solution, serialises the output, and the judge compares with a comparator. **All tests of a run execute in one process** with an in-harness per-test watchdog; only after a timeout does the judge fall back to isolating the remaining tests one per process. | This machine has Python 3.14 and Java 25 but **no Docker**. LeetCode uses the same harness model, so problems feel familiar. One process per run keeps Submit under a second instead of paying JVM/interpreter startup per test. Limits (wall-clock timeout, output cap, Java `-Xmx`, process-tree kill) protect against accidents like infinite loops. Threat model is explicit: the user runs their own code on their own machine. | Docker sandbox (unavailable here; pluggable executor later, P9-2). Per-test process launches (slow). stdin/stdout-only judging (parsing boilerplate, unlike interviews). |
| D4 | **Two test modes and three expectation modes.** Mode `function`: call one method with arguments. Mode `operations`: construct the class, then apply a sequence of `[method, args]` operations and collect every return value (design problems: LRU cache, MinStack, Trie, Union-Find, streams). Expectation `return`, `mutatedArgs` (in-place problems: rotate array, remove duplicates returning k, sort colours), or `both`. | Without these, all design-style problems and every in-place problem are unsupported; that is Batch E and a large part of Batches A–C. | Function-only harness. Per-problem bespoke harnesses. |
| D5 | **Java argument typing by reflection.** The Java harness inspects the declared parameter and return types of the target method and deserialises JSON accordingly from a fixed supported-type table (`int`, `long`, `double`, `boolean`, `char`, `String`, `int[]`, `int[][]`, `long[]`, `double[]`, `char[]`, `char[][]`, `String[]`, `List<Integer>`, `List<List<Integer>>`, `List<String>`, `ListNode`, `TreeNode`, `ListNode[]`). Python deserialises JSON natively with helper conversions for `ListNode`/`TreeNode`. | No codegen step and no duplicated signature metadata; the starter's signature is the source of truth. The table is documented so authors know what they can use. | Codegen of a call site from `meta.json`; a JSON type DSL. |
| D6 | **Custom checkers are TypeScript run in-process by the judge.** Comparators: `exact`, `unorderedList`, `unorderedListOfLists`, `floatTolerance(eps)`, `checker` (a `checker.ts` in the problem dir receiving input, expected and actual). | One implementation, no extra subprocess, unit-testable with Vitest. | Checkers in Python or Java (two implementations or language coupling). |
| D7 | **Problems are git-tracked directories, not database rows.** `problems/<topic>/<slug>/` holds `meta.json`, `statement.md`, `tests.json`, `hints.json`, `editorial.md`, `starter.py`, `starter.java`, `reference.py`, `reference.java`, optional `generator.py`, optional `checker.ts`, optional `assets/`. A validator CLI enforces the schema and runs both references through the judge. | Reviewable in PRs, diffable, authorable by hand or with LLM assistance, and CI can prove every problem is solvable in both languages. | Problems stored in SQLite (not reviewable, no CI guarantee). |
| D8 | **Original problem statements.** Classic problems are rewritten in our own words with our own examples and tests. | LeetCode statements are copyrighted; the underlying algorithms are not. | Copying statements verbatim. |
| D9 | **Authoring pipeline is mandatory infrastructure, built before the catalogue.** Scaffold CLI, a per-problem random-input `generator.py` that uses the reference solution as the oracle to produce hidden tests, the validator as the merge gate, and `docs/AUTHORING.md` with the prompt and checklist used when drafting problems with Claude Code. No runtime LLM dependency for authoring. | ~200 problems × (statement, ≥ 10 hidden tests, hints, editorial, four code files) is the largest cost in the plan; hand-writing hidden tests is the slowest and least reliable part. Generators also make property-style QA (P6-7) free. | Hand-authored tests; an LLM-calling authoring script (redundant with Claude Code plus the validator gate). |
| D10 | **Fine-grained difficulty rating** (1–10) in addition to the Easy/Medium/Hard tier. Easy 1–3, Medium 4–7, Hard 8–10. Default list sort = rating asc, then curriculum topic order. | "Start easy and progress" needs an order finer than three buckets. Rating also drives "next recommended problem". | Tier-only sorting (arbitrary order inside a tier). |
| D11 | **Four progress states** (owner confirmed 2026-09-16): Not started → In progress (**first Run or Submit**; saving a draft does not count) → Solved (Submit accepted) → Mastered (accepted **and** the coach's rubric passes). Status is tracked per language and rolled up as the best status. | The brief asks for "until they are perfect", which is stronger than "accepted". Counting drafts would mark problems In progress merely from browsing. | Binary done/not-done. Draft-triggered In progress. |
| D12 | **Provider-agnostic LLM adapter, bring-your-own key.** v1 providers: **Anthropic** (Messages API, structured output, streaming, prompt caching) and **Google Gemini** (Gemini API, JSON response schema, streaming). Transport per vendor, decided in P5-1: Anthropic through the official `@anthropic-ai/sdk`, Gemini through `fetch`. | Owner chose Anthropic and Gemini (2026-09-16). An adapter keeps prompts provider-neutral and lets tests mock the provider. The asymmetric transport is asymmetric on purpose: streaming structured output from Anthropic means SSE framing, delta variants, mid-stream errors and typed error classes, all of which the vendor already maintains, so hand-rolling it to avoid a dependency would buy a worse implementation; Gemini's streaming half is `data:` lines of JSON, which is genuinely all it is. Both take an injected `fetch`, so neither reaches the network in tests. | Hard-coding one vendor. OpenAI-compatible/Ollama deferred to stretch (P9-4). One transport for both, which would mean either hand-writing Anthropic's stream or adding a second SDK to read four fields off Gemini's. |
| D13 | **Coach is on-demand via an "AI Help" button, never auto-triggered.** It reviews the code written so far plus the latest judge results if any. A local pre-check runs first: if the editor equals the starter or contains no meaningful logic, no API call is made and the panel asks the user to write more. After an Accepted submit the UI nudges (non-blocking) to request a mastery check. **Coach receives:** statement, constraints, target complexity, editorial approach (flagged secret), the user's code, latest judge results, revealed hints, and a summary of prior attempts. **Coach returns** structured JSON: per-dimension scores, markdown feedback, next hint level, `mastered` flag. | Owner decision (2026-09-16): on-demand keeps cost under the user's control and lets the coach help mid-attempt. Structured output lets the UI render a rubric card and lets the status engine set Mastered deterministically. The pre-check avoids paying for empty prompts; the nudge stops Mastered from depending on the user remembering to click. | Auto-trigger on every Run/Submit (cost, noise). Free-form chat only (unparseable, cannot drive status). |
| D14 | **Persistence with `node:sqlite`, hand-written SQL, checked-in migrations.** DB at `data/devpromax.db`. Fallback to `better-sqlite3` only if `node:sqlite` proves unstable on Node 24. | Removes the only native module from the stack, which is the most common Windows install failure on new Node versions. Eight tables do not justify an ORM. | Drizzle + better-sqlite3 (native build risk, ORM overhead). |
| D15 | **Local server hardening.** Bind to `127.0.0.1` only; reject requests whose `Host` is not localhost (DNS-rebinding defence); require a custom header (`X-DevProMax-Client`) on every `/api` request so cross-origin calls fail CORS preflight; only `application/json` bodies accepted. | The server can execute arbitrary code. Any browser tab on the machine could otherwise POST to `/api/run`. | Trusting localhost implicitly. |
| D16 | **Hand-built design system on Radix primitives + Tailwind v4 tokens, components built just-in-time.** Inter for UI, JetBrains Mono for code. One accent colour, neutral greys, 8-pt spacing, no gradients, no hero sections, no emoji as UI, no shadowed card grids. Principles live in `docs/DESIGN.md` and are part of code review. Shortcuts: `Ctrl+Enter` run, `Ctrl+Shift+Enter` submit, `Ctrl+J` toggle bottom panel, `Ctrl+Shift+H` AI Help (`Ctrl+/` is Monaco's comment toggle and is not used). | Slop comes from stock component kits and default palettes. Radix gives accessibility without imposing a look. Building components with the screens that need them avoids designing in a vacuum. | shadcn/ui or MUI out of the box. A full component library before any product screen. |
| D17 | **Test pyramid is mandatory.** Vitest unit; problem contract tests; judge integration tests that spawn real `python`/`java`; React Testing Library component tests; Playwright E2E for golden paths; LLM adapter tests against recorded fixtures (no network in CI). CI: lint, unit and judge integration on `ubuntu-latest` + `windows-latest` per PR; E2E on Ubuntu per PR and on Windows nightly. | The judge is OS-sensitive (process killing, paths, encodings), so Windows must be in CI for it. Windows E2E on every PR is slow and adds little beyond the judge coverage. | Linux-only CI. Full matrix for everything. |
| D18 | **Legacy content archived to `temp/`**, untouched, kept until the app is built out (owner decision 2026-09-16). | The old files are stubs and a CodeSignal drill kit; nothing there feeds the new app, but the owner wants it kept as a reference during the build. | Deleting immediately. |
| D19 | **The API key rests in plaintext in the local SQLite file, with the environment variable as the override.** Recommended by the 2026-09-17 audit; stands unless the owner objects. | The machine is single-user and local (D1) and the key already has to be decryptable by this process without a prompt, so encrypting it with a key stored beside it is theatre. An OS keychain (DPAPI, Keychain, libsecret) needs a native module, which D14 removed from the stack for install-failure reasons. The real exposures are elsewhere and are closed by tasks: the judge child environment (P2-11), logs (redaction is tested), responses (`apiKeyMasked` is structural), exports (P7-5 must never read the settings table). Document the file's location and permissions in ARCHITECTURE. | `keytar`/DPAPI (native module). AES with a local key file (no threat it defends against). |
| D20 | **A coaching conversation is one feedback context plus the follow-ups after it, and chat turns are under the same cap and cost accounting as feedback.** Recommended by the 2026-09-17 audit. | Replaying every stored context makes the eighth follow-up carry eight statements, eight editorials and eight code snapshots, none cacheable, and today none of it is capped. One context per window keeps chat a cheap prose exchange about the review just given; a new AI Help click starts a new window. | Unbounded history (what exists). A token-budgeted sliding window over everything (still replays contexts, and needs a tokeniser). |
| D21 | **A stated constraint is a tested constraint.** Every problem's generator reaches the maximum size the statement gives, and every problem whose target complexity beats the obvious approach carries at least one hidden test that the obvious approach cannot finish within the time limit. The validator warns when the largest hidden input is under half of a stated bound. Recommended by the 2026-09-17 audit. | Three seed problems say 10^5 and test 1000, so the quadratic solution the editorial says will time out passes, and Solved stops meaning what the statement claims. The coach quotes the constraints when explaining complexity, so the claim has to be true. | Constraints as aspiration (what exists). Dropping stated maxima altogether (removes the reason to think about complexity). |
| D22 | **Integers on the wire are bounded to the safe-integer range (\|n\| ≤ 2^53 − 1)** in inputs and expected outputs; the validator rejects a test value outside it. Recommended by the 2026-09-17 audit. | The judge parses results with `JSON.parse`, so two different 64-bit answers that round to the same double compare equal, and Java's reader throws on a Node-stringified 2^63. No planned problem needs full 64-bit answers; sums to 10^18 can be phrased modulo 10^9+7, as interview problems usually are. | A bigint-aware JSON path end to end (harness, protocol, zod, comparators, UI) for problems that do not exist. |
| D23 | **Problem validation is incremental on pull requests and full on `main` and nightly.** A PR validates the problems it changed, unless the judge, a harness or a checker changed, in which case everything. Recommended by the 2026-09-17 audit. | 1.7 s per problem × 200 × two OSes is twelve machine-minutes per push for content that did not change. `main` and the nightly lane keep the whole-catalogue guarantee D7 promises. | Full validation everywhere (slow). Validation only on `main` (a broken problem merges first). |
| D24 | **In production one Fastify process on one port serves both the built UI and the API.** `npm start` sets `NODE_ENV=production`, serves `apps/web/dist` with an SPA fallback, and shuts down on `SIGINT`/`SIGTERM`. Recommended by the 2026-09-17 audit. | Today `npm start` runs the API alone and nothing serves the UI. One origin means no CORS, the Host allow-list covers page loads too, and the client header stays an `/api` rule. | A second static server on another port (CORS, two processes to stop). Vite preview in production (dev tooling in the start path). |
| D25 | **A manual override can raise a status to Solved at most; Mastered is only ever set by the coach.** Recommended by the 2026-09-17 audit, implemented by P5-10: the override clamps rather than refuses, so the highest status that is the user's to claim is still granted. D11's wording is superseded here. | "I solved this on paper" is a real claim the user is entitled to make. "The coach passed this" is not something the user can assert, and an override to Mastered today unlocks the editorial and the `solution` rung with no submission behind it, which is the one thing D13 exists to prevent. | Override to any state (what exists). No override at all (punishes solving elsewhere). |

---

## 3. Planned repository layout

```
.
├── apps/
│   ├── web/                 React + Vite + Monaco; Playwright E2E lives here
│   └── server/
│       └── src/
│           ├── api/         Fastify routes and the services behind them
│           ├── db/          node:sqlite, migrations, repositories
│           ├── judge/       executors (python, java), harness sources, comparators, limits
│           ├── problems/    loader, schema validation, validator + authoring CLIs
│           └── coach/       provider adapters, prompts, context builder
├── packages/
│   └── shared/              Domain types + zod schemas shared by web and server
├── problems/                Content: problems/<topic>/<slug>/...
├── docs/                    ARCHITECTURE.md, PROBLEM_FORMAT.md, AUTHORING.md, DESIGN.md, COACH_PROMPTS.md, CURRICULUM.md (P6-1, not yet written)
├── data/                    Runtime SQLite (`devpromax.db`) + judge workspaces (gitignored)
├── temp/                    Archived legacy content (see D18)
├── ROADMAP.md               This file
└── CLAUDE.md                Working agreement for AI-assisted development
```

---

## 4. Curriculum (14 topics, learning-path order)

Foundation: Arrays → HashMap → Sorting → Binary Search · Linear: Linked List → Stack → Matrix · Non-linear: Binary Tree → Heap → Graph · Techniques: Backtracking → Dynamic Programming → Bit Manipulation · Advanced: Data Structures (Trie, Segment Tree, Fenwick, Union-Find, LRU/LFU)

Target catalogue for v1: **~200 problems** (roughly 14 per topic: 5 Easy, 6 Medium, 3 Hard). Three hand-written pilot problems pin the format, twenty seed problems ship with the vertical slice, then batches.

---

## 5. Task table

Phases: **P0** Foundations · **P1** Problem format · **P2** Judge + authoring pipeline · **P3** API + persistence · **P4** Web UI · **P5** LLM coach · **P6** Catalogue scale-up · **P7** Learning features · **P8** Hardening & release · **P9** Stretch

| Priority | ID | Phase | Task | Execution plan | Status |
| --- | --- | --- | --- | --- | --- |
| 1 | P0-1 | P0 | Archive legacy content | `git mv` `DSA/`, `Interview Asssessment/`, `.github/` and the old `README.md` into `temp/` so history is preserved. Do not edit anything inside `temp/`. | Done |
| 2 | P0-2 | P0 | Write ROADMAP.md and CLAUDE.md | Capture goals, architecture decisions with reasoning, repo layout, curriculum, and this task table. CLAUDE.md records conventions, commands and the working agreement. | Done |
| 3 | P0-3 | P0 | Resolve open questions with the owner | Answered 2026-09-16: app name **DevProMax**; providers Anthropic + Gemini; local subprocess judge; TypeScript backend; Mastered accepted; ~200 problems for v1; coach on-demand via "AI Help" button; `temp/` kept until the app is built out. | Done |
| 4 | P0-4 | P0 | Scope audit | Audit the plan for sequencing errors, functional gaps and cuttable scope; apply the results (see revision history). Outcomes: D2–D6, D9, D11, D13–D17 added or changed; validator moved after the judge; walking-skeleton milestone M0 added; OpenAPI, ORM, early command palette, format-on-save and list virtualisation cut or deferred. | Done |
| 5 | P0-5 | P0 | Scaffold monorepo and tooling | npm workspaces: `apps/web`, `apps/server`, `packages/shared`. TypeScript strict, ESLint (typescript-eslint, react-hooks, jsx-a11y), Prettier, Vitest root config, Playwright in `apps/web`, pino logging in the server, `.editorconfig`, `.nvmrc`, expanded `.gitignore` (`data/`, `node_modules/`, build outputs). Root scripts: `dev`, `start`, `build`, `test`, `test:e2e`, `lint`, `typecheck`, `problems:validate`, `problems:new`, `problems:gen`. | Done |
| 6 | P0-6 | P0 | CI pipeline | GitHub Actions: per-PR matrix `ubuntu-latest` + `windows-latest` for lint, typecheck, unit, problem contract, judge integration; E2E on Ubuntu per PR; nightly E2E on Windows. `setup-node` 24, `setup-python` 3.12, `setup-java` 21. Cache npm. Required status checks on `main`. | Done |
| 7 | P0-7 | P0 | Shared domain model | In `packages/shared`: zod schemas + inferred types for `ProblemMeta`, `TestCase`, `TestMode` (function / operations), `ExpectMode` (return / mutatedArgs / both), `Comparator`, `Language`, `RunRequest`, `RunResult`, `Verdict` (AC / WA / TLE / RE / CE, plus MLE for Java OutOfMemoryError only), `Submission`, `ProgressStatus`, `CoachFeedback`, `Settings`. Unit tests for schema edge cases. | Done |
| 8 | P0-8 | P0 | Design principles and tokens | `docs/DESIGN.md` principles (see D16), ending in a review checklist. Tailwind v4 theme tokens in `apps/web/src/styles/tokens.css`: OKLCH ramps (neutral + one accent + success/warn/danger) under semantic aliases, type scale, 8-pt spacing, radii, one overlay shadow, focus-ring utility. Light/dark via `data-theme`, system when absent. Inter and JetBrains Mono self-hosted. Four primitives (Button, Input, Tabs, Tooltip) on Radix, RTL-tested, plus the dev-only `/dev/kitchen-sink` page; further components are built with the screens that need them. Contrast is measured from the token file by `contrast.test.ts` rather than asserted in prose. | Done |
| 9 | P1-1 | P1 | Problem package specification | `docs/PROBLEM_FORMAT.md` + JSON schema. `meta.json`: id, slug, title, version, topic, patterns[], tier, rating (1–10), order, mode (function / operations), entry (method name, or class name for operations), expect (return / mutatedArgs / both), comparator (+ eps), limits per language (Python default 2× Java), related[]. `statement.md` (objective, input/output, constraints, worked examples); `tests.json` with `samples[]` (each with `explanation`) and `hidden[]`; wire format for primitives, strings, arrays, nested arrays, matrices, `ListNode` (array form), `TreeNode` (level-order with nulls), graphs (edge lists); operations tests as `[[method, args], ...]` → `[returns...]`. Starter conventions: Python starters import `typing` and show `ListNode`/`TreeNode` definitions as comments; Java starters `import java.util.*;`. Optional `generator.py`, `checker.ts`, `assets/`. | Done |
| 10 | P1-2 | P1 | Static validator | `npm run problems:validate --static [slug]`: schema validation, unique ids/slugs, both languages present, referenced files exist, sample count ≥ 3 and hidden ≥ 10, comparator/expect combinations legal, file-pointed error messages. Reference execution is added in P2-7. | Done |
| 11 | P1-3 | P1 | Three hand-written pilot problems | One `function`/`return` (e.g. two-sum style), one `function`/`mutatedArgs` (e.g. in-place rotate), one `operations` design problem (e.g. min-stack). Full packages in both languages. These pin the format and are the fixtures for judge tests and the walking skeleton. | Done |
| 12 | P2-1 | P2 | Judge core | `apps/server/src/judge`: `runProblem({problem, language, code, tests, mode})` → isolated workspace under `data/judge/<uuid>/`, executor selection, **all tests in one process** with the harness reporting per-test `{verdict, expected, actual, stdout, stderr, timeMs}` over a dedicated result file; on a per-test timeout the judge kills the process and re-runs the remaining tests isolated one per process. Always cleans up. Concurrency queue (default 2). | Done |
| 13 | P2-2 | P2 | Python executor | `runner.py` harness: import user module, instantiate `Solution`, dispatch `function` or `operations` mode, capture return and/or mutated args per `expect`, serialise via result file (user `print` never corrupts results), report exceptions with traceback, per-test watchdog. Spawn `python -X utf8 -I` with workspace on `sys.path`, stdin closed, recursion limit 10⁴. Minimum supported Python 3.10. Audit 2026-09-17: `-I` removes the script directory from `sys.path`, so "workspace on `sys.path`" above is wrong; the solution is loaded by path, which is why it works. | Done |
| 14 | P2-3 | P2 | Java executor | `Main.java` harness with a dependency-free JSON reader/writer, reflection-based argument typing (D5 table), `ListNode`/`TreeNode` helpers, both test modes and all expect modes, per-test watchdog thread. `javac --release 21 -encoding UTF-8 -d <ws>` once per run with its own timeout; `java -Xmx256m -Xss64m -Dfile.encoding=UTF-8 -cp <ws> Main` once per run. OutOfMemoryError → MLE. Compile errors parsed into `{line, column, message}` for editor markers. | Done |
| 15 | P2-4 | P2 | Comparators and checkers | `exact`, `unorderedList`, `unorderedListOfLists`, `floatTolerance(eps)`, and `checker` loading the problem's `checker.ts` in-process (input, expected, actual → pass/fail + message). Unit-tested with edge cases (nested nulls, -0.0, large ints). | Done |
| 16 | P2-5 | P2 | Limits, safety and local-server hardening | Wall-clock timeout per test (defaults 2 s Java / 4 s Python, overridable per problem), compile timeout, kill full process tree on timeout (`taskkill /T /F` on Windows, process group on POSIX), stdout/stderr cap 64 KB, workspace paths never derived from user input. Server: bind `127.0.0.1`, `Host` allow-list, required `X-DevProMax-Client` header, JSON-only bodies. Threat model documented in `docs/ARCHITECTURE.md`. | Done |
| 17 | P2-6 | P2 | Run vs Submit semantics | Run = sample tests + user custom cases (one JSON value per argument, validated against the starter signature); results not persisted as submissions but mark In progress. Submit = all tests, persisted with code snapshot, verdict, timing and problem version; Accepted → Solved; on failure the first failing hidden test's input, expected and actual are revealed. | Done |
| 18 | P2-7 | P2 | Full validator (reference execution) | Extend `problems:validate` to run both reference solutions through the judge and require every test to pass; starters must compile/import. Wired into CI as the problem-contract job. | Done |
| 19 | P2-8 | P2 | Judge test suite | Unit: comparators, serialisers, compile-error parser, verdict aggregation, custom-input parser. Integration (spawns real interpreters, both OSes): AC, WA with diff, TLE mid-batch with isolation fallback, RE, CE, MLE (Java), stdout noise tolerated, stdin read fails cleanly, deep recursion, output cap, operations mode, mutatedArgs mode. | Done |
| 20 | P2-9 | P2 | Authoring pipeline | `npm run problems:new <topic> <slug>` scaffolds a package; `npm run problems:gen <slug>` runs `generator.py` (inputs only, seeded from the slug so reruns are byte-identical), answers every case with the Python reference, then cross-checks the result against the Java reference through the problem's own comparator, and bumps `meta.version` only when the tests actually changed. Validator as the gate, with a new rule: a surviving `TODO` from the scaffold is an error, so an unfinished package cannot look valid. `docs/AUTHORING.md` holds the drafting prompt and checklist used with Claude Code: originality, realistic constraints, edge cases (empty, single, max size, duplicates, negatives), difficulty calibration, both languages idiomatic. | Done |
| 21 | P2-10 | P2 | Seed 20 problems | Using the pipeline: 20 original problems (the 3 pilots plus 17 new) across Arrays, HashMap, Sorting, Binary Search and Stack, 7 Easy and 13 Medium, covering two pointers, sliding window, prefix sum, frequency map, monotonic stack, boundary binary search and binary search on the answer, with three in-place and two design (`operations`) problems and one `unorderedListOfLists` comparator. 60 samples and 299 hidden tests, every hidden test computed by the Python reference and agreed by the Java one. All pass the full validator. | Done |
| 22 | P3-1 | P3 | Fastify API | Routes: `GET /api/problems?topic&tier&status&q&language`, `GET /api/problems/:slug`, `GET /api/problems/:slug/assets/*`, `POST /api/run`, `POST /api/submit`, `GET /api/problems/:slug/submissions`, `GET /api/progress`, `PUT /api/progress/:slug/:language` (manual override), `PUT`/`DELETE /api/drafts/:slug/:language`, `GET/PUT /api/settings`. Request/response validated with shared zod schemas (`packages/shared/src/api.ts`); one error envelope for every failure; hardening from P2-5 applied as a plugin. The two coach routes (`POST /api/coach/feedback` SSE, `POST /api/coach/chat`) move to P5-1/P5-3, which own the provider's streaming half — stubs would have put two endpoints in the contract that answer nothing. | Done |
| 23 | P3-2 | P3 | SQLite persistence | `node:sqlite`, hand-written SQL, migrations checked in and applied at startup. Tables: `submissions` (with `problem_version`), `drafts`, `problem_progress` (per slug+language), `coach_sessions`, `coach_messages`, `notes`, `settings`, `events` (for streaks/activity). Repository layer with unit tests against an in-memory DB. | Done |
| 24 | P3-3 | P3 | Progress status engine | Pure function deriving `NotStarted / InProgress / Solved / Mastered` per problem+language from transitions (run, submit accepted, coach mastered, manual override); draft saves do not change status. Roll-up across languages = best status. Exhaustive unit tests, including regressions (a later WA never demotes Solved). | Done |
| 25 | P3-4 | P3 | Settings and API key handling | Provider (Anthropic / Gemini), model, API key, judge timeouts, spend cap, editor preferences (font size, tab size, Vim keybindings), last-used language, reset-all-progress. Key stored in local DB (masked in UI, never logged, excluded from exports), env var override `COACH_API_KEY`. "Test connection" endpoint. Delivered with a `CoachProvider` adapter (`apps/server/src/coach/`) whose `testConnection` half exists now and whose `stream` half is P5-1's; reset-all-progress clears practice (submissions, progress, drafts, events, coach sessions) and keeps notes and settings. | Done |
| 26 | P3-5 | P3 | API tests | Fastify `inject` tests per route: validation errors, hardening rejections (bad Host, missing header), filtering/sorting correctness, submit→progress transition, draft round-trip, settings masking, asset path traversal. Judge and `fetch` are stood in for; everything below the routes is real. | Done |
| 27 | P4-1 | P4 | Walking skeleton (M0) | Problem list and workspace, React Router, TanStack Query, a typed API client that carries the `X-DevProMax-Client` header, and Monaco bundled locally (core plus the Python and Java tokenizers only - importing all of Monaco pulled in nine megabytes of workers for languages the judge does not run). Run and Submit wired to the judge, results per test with the hidden-test reveal policy visible. Built on the P0-8 tokens rather than throwaway styling, since those landed first. Exit criterion is `apps/web/e2e/m0.spec.ts`: all three pilots accepted in both languages through the real UI, plus the list flipping to Solved without a reload. | Done |
| 28 | P4-2 | P4 | App shell | Routing (`/`, `/problems/:slug`, `/progress`, `/settings`) with React Router, top bar with global progress, theme toggle, shortcut registry (`Ctrl+Enter` run, `Ctrl+Shift+Enter` submit, `Ctrl+J` toggle panel, `Ctrl+Shift+H` AI Help). TanStack Query for server state. Command palette deferred to P7-7. The shell is a layout route, so the top bar does not blink while a problem loads. The registry listens in the capture phase (Monaco stops propagation of the keys it handles) and leaves an unclaimed shortcut to the browser. Theme is a server setting mirrored into `localStorage` so the first paint is right. `/progress` and `/settings` are real screens rather than stubs: the dashboard's richer half stays P7-5's and the coach's key stays P5-3's, but the editor preferences P4-6 reads had to be editable somewhere. `Ctrl+Shift+H` is in the table with nothing bound to it until P5-3. | Done |
| 29 | P4-3 | P4 | Statement renderer | Sanitised markdown renderer for statements, editorials, hints and notes: fenced code with highlighting, tables, inline math via KaTeX (optional, lazy), images from `problems/<slug>/assets/` served by the API. Snapshot tests on the pilot statements. `react-markdown` with no raw-HTML plugin, so markdown becomes React elements and never `innerHTML`, plus `rehype-sanitize` as the second lock; plugin order is sanitise first, then the two plugins that generate markup. Highlighting is `lowlight` with Python and Java registered by hand rather than `rehype-highlight`, which bundles forty grammars to get two - the same reasoning as the Monaco setup. The highlight theme is the app's one accent (docs/DESIGN.md section 3): green, amber and red are spoken for by verdicts. KaTeX is fetched only when the text contains maths. | Done |
| 30 | P4-4 | P4 | Problem list page | Plain scrollable table (virtualise only if P8-2 shows a need): status icon, title, topic, patterns, tier, rating, last attempted. Default sort rating asc then curriculum order; sortable columns; search on title/patterns. Row click opens the workspace, with the title as the link that makes the row reachable by keyboard. Sorting and search happen on the server, because the default order *is* the curriculum and only the catalogue knows it. Status is drawn as a shape plus a word, and "Not started" is visually hidden - it would otherwise print two hundred times down the left edge - while staying in the accessible name. The sticky header says how many rows are shown out of the catalogue; the solved count lives once, in the shell's top bar. | Done |
| 31 | P4-5 | P4 | Filters | Sidebar filters: topic (multi, with per-topic solved/total counts), tier, status (Not started / In progress / Solved / Mastered), language solved-in. All filters persisted in URL query - the URL is the state, so a filtered list is bookmarkable and the back button works through a session of narrowing down. Values are checked against their enum one at a time, so a hand-edited `?topic=arrayz` drops that word instead of resetting the query. Counts come from the unfiltered catalogue and do not move as the list narrows. Clear-all keeps the sort, which is not a filter. Empty-state copy distinguishes "nothing matches" from "the catalogue is empty". | Done |
| 32 | P4-6 | P4 | Problem workspace | Resizable split: left tabs Description / Hints / Editorial (locked until Solved) / Submissions; right Monaco (lazy-loaded) with language switch, starter code, reset-to-starter behind a confirmation, debounced draft autosave that invalidates nothing because a draft moves no status (D11), and editor preferences from settings including Vim mode, imported only when the preference is on. Bottom panel Testcases (samples plus a custom-input editor validated by the same `parseCustomTests` the server runs, so the message while typing is the message the server would have given) / Results. Layout persisted in localStorage, which is where a window's shape belongs rather than in settings. The splitter is ARIA's window-splitter pattern: focusable, arrow keys, Home/End. **Notes and Coach tabs are deliberately absent** - the API has no notes route until P7-4 and the coach's stream arrives with P5-1, and a tab that cannot save what you type into it is worse than no tab. | Done |
| 33 | P4-7 | P4 | Run and Submit results UI | Verdict banner (AC/WA/TLE/RE/CE/MLE) with timing; a test list beside the selected test's detail, opening on the first failure rather than on test 1; expected beside actual with the differing lines marked, plus a "first difference at `[4]`" line, which is what stops a user scanning two hundred-element arrays by eye; mutated-args and operations views (the latter one row per call); stdout/stderr behind a disclosure; compile errors drawn as Monaco markers and clickable to jump to the line; Previous/Next failure controls, reachable by Tab. Values are capped for display, because a revealed hidden test can be a hundred thousand integers. | Done |
| 34 | P4-8 | P4 | Live status propagation | On Accepted, invalidate list/progress queries so the row flips to Solved without reload; subtle non-blocking confirmation (no confetti). Per-topic progress bars in sidebar update. The confirmation is the workspace header's status mark itself, read from the refetched problem rather than from the run result in state, so it is also right on arrival for a problem solved yesterday; it sits in a `role="status"` region so the flip is announced rather than only seen. `StatusMark` moved to `src/ui/` on its second caller (DESIGN.md section 7). | Done |
| 35 | P4-9 | P4 | UI tests | The RTL half landed with P4-2..P4-7, as CLAUDE.md requires (`shortcuts/`, `markdown/`, `screens/problems/`, `screens/workspace/`, `app/AppShell.test.tsx`, over the fake server in `src/test/harness.tsx`): filters, sort, status marks, results diff, shortcuts, custom-input validation, draft autosave, reset-to-starter. The Playwright golden path is `e2e/flows.spec.ts`: filter Easy+Arrays → open → hints → Run → Submit → Solved in the list → reload persists, plus the same loop by keyboard alone. It asserts the *submission count moves* rather than that the status reads Solved, because the suite runs against whatever history the database already holds. `DEVPROMAX_DB` (new, in `config.ts`) points the suite at `data/e2e.db` so those submissions stop landing in the developer's own practice history. | Done |
| 36 | P4-10 | P4 | Design polish and a11y pass | Review every screen against `docs/DESIGN.md`; loading skeletons, empty states, error states, focus rings, 1024 px minimum width behaviour, axe audit with zero serious violations, colour-contrast check for both themes. `Loading`/`Skeleton` and `ErrorState` join the primitives (the fourth screen was about to write the fourth copy of "Loading…"); the 1024 px floor is CSS-only in the shell; `e2e/a11y.spec.ts` runs axe over four screens × two themes and gates on serious and critical. Findings fixed: a tablist containing the panel toggle, unnamed progressbars, and "Arrays1/4" as a topic's accessible name. `docs/DESIGN.md` gains sections 10 and 11 and the checklist moves to 12. | Done |
| 37 | P5-1 | P5 | LLM provider adapter | `CoachProvider.stream(options) → AsyncIterable<string>` yielding raw JSON text, with `streamCoachFeedback` turning that into markdown deltas plus one validated `CoachFeedback`. Anthropic goes through the official `@anthropic-ai/sdk` (adaptive thinking, `output_config.format` from the zod schema, `cache_control` on the static system prompt); Gemini stays on `fetch` against `:streamGenerateContent?alt=sse` with `responseSchema`. Failures become `CoachProviderError` with a user-facing message and a `retryable` flag, never a vendor status code. The provider yields *text* rather than parsed objects because a structured document is incomplete until its last chunk: `partialJson.ts` reads `feedbackMarkdown` out of the partial JSON so the panel can paint prose while the scores are still arriving, which is what lets one request serve both the rubric card and the streaming panel. Fixtures only, no network (D17). | Done |
| 38 | P5-2 | P5 | Coach prompt design | `docs/COACH_PROMPTS.md` + `apps/server/src/coach/prompts/v1/system.md`, read once at load and recorded as `PROMPT_VERSION`. Prose in a `.md` file rather than a template literal: it is the part of the app most likely to be edited by arguing about a sentence, and it is the bytes `cache_control` points at, so it has to stay identical across requests. The five rubric dimensions, the 0–4 scale with 4 reserved, and the hint ladder with its two hard gates (`solution` needs Solved **and** an explicit ask; the prose may not route around the ladder by writing the method out). `context.ts` assembles the user turn in a fixed order that is also the reverse of the drop order, under a character budget rather than a token one - a tokeniser per vendor for a limit that only has to be conservative would be precision bought for nothing. The user's code is never truncated: a review of half a function is confidently wrong about what is missing, so an oversized request the provider rejects is the better failure. | Done |
| 39 | P5-3 | P5 | "AI Help" button and Coach panel | `POST /api/coach/feedback` and `POST /api/coach/chat` as event streams, completing the surface P3-1 left open, plus the Coach tab the P4-6 row deliberately omitted. The pre-check (`coach/precheck.ts`) runs before the key lookup and long before any request, so the cheapest true answer wins: someone with no key who has also not started is told to write code, not sent to Settings. AI Help is a ghost button and opens the Coach tab as it starts - a turn streaming into a hidden tab looks like a button that did nothing. The panel paints prose first and lands the rubric card at the end, which is what the partial-JSON reader in P5-1 exists for. SSE means failures after the first frame travel as `error` events rather than statuses, and a local refusal is a `skipped` event on a 200 because nothing went wrong. Audit 2026-09-17: the Settings coach section that this row and P5-6 each left to the other never shipped - P5-8. | Done |
| 40 | P5-4 | P5 | Mastery decision | `applyMastery` in the coach service, behind three gates that must all hold: the coach sets `mastered`, `meetsMastery` re-checks every dimension against the threshold, and the status engine refuses `coach_mastered` below Solved. The flag is a claim and the scores are the evidence, so disagreement resolves against the claim. A failed check writes `coach_not_mastered`, which changes nothing - declining to promote is not grounds to remove a status already earned, and no judge ran so it is not an attempt either. The nudge after an accepted submit is a `role="status"` strip above the result with two buttons, not a dialog: interrupting the moment a problem is solved to ask for money is the wrong thing to do with that moment. Manual override and the Mastered filter already existed (P3-1, P4-5). | Done |
| 41 | P5-5 | P5 | Progressive attempt memory | The feedback half shipped with P5-3; this adds the deltas. Migration `002` puts a code snapshot on the coach turn - not derived from `submissions`, because the code someone had on screen when they asked for help is usually neither their last submission nor their next one. `codeDelta.ts` diffs it against the code in front of the coach now and `describeDelta` renders it, so the prompt gets `- old` / `+ new` lines rather than the previous file: sending both versions would let the model work it out, at roughly the size of the solution in tokens per request, none of it cacheable. The valuable case is the one that produces no diff at all - someone asking twice without editing is stuck, and the prompt now tells the coach to change angle rather than rephrase. | Done |
| 42 | P5-6 | P5 | Cost, privacy and fallback | `packages/shared/src/cost.ts` so the estimate under the button and the figure the cap enforces are computed the same way. The table will go stale and fails safe: an unknown model is charged at the dearest rate known for its provider, so a missing price trips the cap early. `COACH_DEFAULT_MODEL` moved to shared because a null model is not an unknown one - it resolves to a specific model, and pricing it as unknown would overstate the default configuration nobody changes. Real usage comes from each vendor's own counts and is stored per turn (migration `003`); NULL means unreported, never free. The cap is per conversation and checked before a turn, so it is a floor the next turn may cross rather than a ceiling. `judgeSummary` is the no-key floor, and asserts only what the result proves. `REDACT_PATHS` is now tested against a real pino instance. | Done |
| 43 | P5-7 | P5 | Coach test suite | Most of what this row lists shipped with the tasks that needed it, per CLAUDE.md - fixtures in `stream.test.ts`, prompt assertions in `prompts.test.ts`, parse failures in `feedback.ts`'s tests, a mocked stream in `coach.test.tsx`, Mastered transitions in `coachService.test.ts`. What was left were the two gaps those leave: `contract.test.ts` runs one set of assertions against **both** providers, because D12 entitles everything above `coach/` to assume they are interchangeable and two separately-written suites drift; and `e2e/coach.spec.ts` is the only thing that reads a real `text/event-stream` off a real socket, covering the two paths the server answers alone (pre-check, no key) so no fake provider has to be wired into the production server to test it. 214 coach tests in all. | Done |
| 44 | P5-8 | P5 | Coach settings (the missing key field) | Audit 2026-09-17: `Settings.tsx` still says the provider, model and key "belong with P5-3 and P5-6", and neither shipped them, so the panel's "Open Settings" route lands on a page with nothing to fill in, and the only ways to configure the coach are `COACH_API_KEY` or a hand-made PUT. Add a Coach section to `/settings`: provider (Anthropic / Gemini), model (free text with the provider's default shown), the API key as a write-only field (the view already returns `apiKeyMasked` and `apiKeySource`, so the field shows `sk-…3f9a` and "from environment" when the env var wins, and the key never round-trips), Clear key, spend cap per conversation, and Test connection calling `POST /api/settings/test-connection` with the result inline. RTL: the masked value never equals the typed value after save. `e2e/coach.spec.ts` gains no key → Settings → paste key → return → AI Help enabled, with a fake key that fails Test connection honestly; `coach.spec.ts:85` asserts only the URL today, which is how this went unnoticed. | Done |
| 45 | P2-11 | P2 | Judge child environment and harness hardening | Audit 2026-09-17, confirmed: `process.ts:81` spawns with `env: options.env ?? process.env` and no executor passes `env`, so `print(os.environ["COACH_API_KEY"])` returns the key in `stdout`, which the API returns and the results panel renders. Build the child environment from an allow-list in each executor (`PATH`, `SYSTEMROOT`, `SYSTEMDRIVE`, `TEMP`, `TMP`, `USERPROFILE`, `HOME`, `JAVA_HOME`, `LANG`, `LC_ALL`, `PYTHONIOENCODING`) and never spread `process.env`; integration test in both languages that prints every variable and asserts the key and every `DEVPROMAX_*` name are absent. Same files, same task: (a) javac is given `Solution.java` before `Main.java`, so a user helper class named `Main`, `Json`, `Convert`, `ListNode` or `TreeNode` produces a duplicate-class diagnostic on the harness file and is reported as "the judge's harness failed to compile" - rename the harness classes to `DevProMax*` and map a duplicate-class diagnostic that names one of them to a user-facing CE ("the judge reserves this class name"); (b) `workspace.dispose()` can reject from the `finally` in `runProblem` and turn a finished verdict into a 500 - Windows holds fresh `.class` files briefly - so log and swallow inside `dispose` and raise `maxRetries`; (c) the `taskkill` child has no `error` handler, so a spawn failure is an unhandled event and the queue slot never frees - add the handler and a fallback force-kill that settles the promise; (d) a compile that hits its timeout is reported as "compilation failed" - map `killed` to "the compiler exceeded N s". | Done |
| 46 | P5-9 | P5 | Coach money path | Audit 2026-09-17, each confirmed in `routes/coach.ts` and `coachService.ts`. (1) Nothing cancels the vendor request when the browser aborts: `pipe` never listens for `request.raw.on('close')` and no `signal` reaches `provider.stream`, so Stop, a second AI Help click and navigating away all bill the full turn and store it as a coach message. Create an `AbortController` per request, abort it on `close`, thread the signal through `streamFeedback` and `streamChat` into `streamCoachFeedback`, call `events.return()`, and do not persist an aborted turn. (2) `/api/coach/chat` checks no cap and records no cost, so a tripped cap is bypassed by asking follow-ups; apply `overSpendCap` and `onUsage` there exactly as feedback does. (3) Chat replays every stored message, and every AI Help click appends a full 120k-character context to the one session, none of it cacheable; adopt D20 - window the history to the latest feedback context plus the turns after it - and add New conversation (the skip message already tells the user to start one and no route lets them). (4) Usage is reported only after a stream completes, so a timeout or mid-stream error is recorded as free; report accumulated usage in a `finally`, and when summing treat a NULL `cost_usd` as the dearest known rate rather than zero. (5) Anthropic cost ignores `cache_creation_input_tokens` and `cache_read_input_tokens`, which is where the whole system prompt is billed; add them at 1.25× and 0.1× and correct the claim in `cost.ts` that caching only lowers the bill. (6) `max_tokens: 8192` includes adaptive thinking, so a long think truncates the JSON and the "cut off, retryable" retry does it again at full price; raise the budget, set a medium effort, turn `stop_reason: 'max_tokens'` into a non-retryable message, and raise `STREAM_TIMEOUT_MS` to match. (7) Gemini: `AbortSignal.timeout` rejects with `TimeoutError`, which `describeNetworkError` reports as offline; and the user turn is persisted before the call, so one failed turn leaves `user, user` in the history, which Gemini rejects with a 400 on every later turn - persist the user turn with its reply, or merge same-role turns in the adapter. Tests for each: disconnect asserts `events.return()` and the provider signal; chat cap; windowing; usage on throw; cache-token fixture; `max_tokens` fixture; a `user, user` Gemini body. | Done |
| 47 | P4-11 | P4 | Draft integrity in the workspace | Audit 2026-09-17, confirmed in `Workspace.tsx` and `hooks.ts`. (1) `useSaveDraft` writes nothing into the query cache, and the editor is re-seeded from `problem.drafts[language]` on every language change, so Java → Python → Java restores the starter over ten minutes of typing and the next autosave persists that starter over the real draft: write the saved draft through with `setQueryData` on success (still no invalidation - D11 stands) and the same for delete. (2) The debounce is cleared on unmount, language switch and slug change without a flush, so the last 800 ms of typing before Back or a language click never reach the server: flush from refs in the cleanup, and send a `keepalive` request on `pagehide`. (3) `persisted` is set before the PUT resolves and a failed save is never retried; set it in `onSuccess`, and cancel the pending timer before reset-to-starter's DELETE so the two cannot race. (4) A run or submit that resolves after the user changed language or problem lands its result, tab switch and mastery nudge on the wrong screen: bail in `onSuccess` when `variables.slug` or `variables.language` no longer match, and disable the language group while busy. RTL tests for the round-trip, the flush and the mismatch; `flows.spec.ts` gains "type, switch language twice, reload: both drafts survive". | Done |
| 48 | P4-12 | P4 | Coach panel ownership, shortcut scope, panel state, error boundary | Audit 2026-09-17. (1) `useCoach.ts` resets `phase` to idle in every turn's `finally`, so a second turn started mid-stream (`Ctrl+Shift+H` stays enabled while streaming, as do Check it and Try again) is marked idle by the first turn's abort rejection while its text keeps arriving: capture the controller in `consume` and reset only when it is still the current one. (2) The shortcut registry has no focus or dialog check: `Ctrl+Enter` in the coach follow-up field runs the judge, and with the reset confirmation open it runs the code behind the dialog; skip when the target is inside `[role=dialog]` or `[role=alertdialog]`, and let the follow-up form claim `Ctrl+Enter` itself. (3) Radix unmounts inactive tab panels, so revealed hints reset when the user looks at the description, the results selection resets when flipping to Testcases, and a half-typed follow-up is lost: lift `revealed` into `Workspace` (P7-1 needs it there to send `revealedHints`, which is always 0 today) and `forceMount` plus `hidden` on the two stateful panels. (4) There is no error boundary anywhere: a failed `CodeEditor-*.js` fetch after a server restart is a blank page; add one around the shell's outlet and one around the editor with a reload action, both drawn with `ErrorState`. | Done |
| 49 | P3-6 | P3 | Production entry point | Audit 2026-09-17: `npm start` builds the web app and then runs the API alone - nothing serves `apps/web/dist`, so the command CLAUDE.md lists as "production build + serve" opens nothing; `NODE_ENV` is never set, so the catalogue cache is off and `pino-pretty` (a devDependency) is loaded at startup; and no signal handler calls `app.close()`, so Ctrl+C never runs the `onClose` hook that closes the database, and in-flight judge children are orphaned. Adopt D24: serve `apps/web/dist` from the same Fastify process with `@fastify/static` under the same Host check (the client header stays an `/api` rule - a page load cannot carry it) with an SPA fallback for the router's paths; set `NODE_ENV=production` in the start script; handle `SIGINT` and `SIGTERM` by closing the app and killing live judge process trees; reject a non-numeric `DEVPROMAX_PORT` and a port in use with a message; print the URL (opening the browser is P8-3's first-run work). E2E: `smoke.spec.ts` gains a variant that runs against `npm start` rather than Vite. | Done |
| 50 | P2-12 | P2 | Harness typing gaps before linked lists | Audit 2026-09-17, confirmed on the installed Python: `runner.py` names an annotation with `__name__`, which turns `Optional[ListNode]`, a `ListNode`-or-`None` union and `List[ListNode]` into `Union` or `List`, so `decode_argument` hands the solution a raw list. No catalogue problem hits it yet; every linked-list and tree starter in Batch B and C will. Use `typing.get_args` recursively so `Optional`, unions and `List[ListNode]` decode, add a Python path for `ListNode[]` (in the D5 table for Java, absent in Python), and add a linked-list and a tree problem to the judge integration fixtures in both languages before P6-3 starts. Same task: integers are compared after `JSON.parse`, so two different `long` answers that round to the same double compare equal, and Java's reader throws on a Node-stringified value at 2^63; adopt D22 and enforce the bound in the validator on every test value. Also: Java `char[]` conversion calls `charAt(0)` on an empty string, `int` parameters silently truncate `2.5`, an exception mid-sequence in operations mode discards the returns so far without saying which call failed (include op index and method name), and `mode: operations` accepts an `expect` other than `return` with no schema refinement. `-I` drops the script directory from `sys.path`; the P2-2 row's wording is corrected in place. | Done |
| 51 | P2-13 | P2 | Output and time bounds in the harnesses | Audit 2026-09-17. (1) Both harnesses buffer a test's prints without bound and cap only afterwards, so a print loop runs to the timeout, and in Java hits `-Xmx` first and is reported as MLE; the comment in `runner.py` claims the opposite. Replace the buffers with capping streams that drop writes past 16 KB. (2) Returned values have no cap at all: a wrong subsets-style answer of tens of megabytes is serialised whole, parsed whole by zod and sent to the browser; cap the JSON line in both harnesses at 2 MB and report it as a serialisation error. (3) The Python watchdog is a `threading.Timer`, which cannot fire while a C call holds the GIL, so a catastrophic regex or `[0] * 10**9` is bounded only by the judge's outer `tests × perTest + 5 s` - 85 s for twenty hidden tests - before isolation adds another round. In `executeAll`, watch `results.jsonl` and kill the process when no record arrives within `perTestMs` plus slack, then isolate as today. (4) `os.fsync` after every record costs milliseconds each on Windows and buys nothing since the reader runs after exit; flush is enough. Integration tests: a print storm in both languages ends in TLE not MLE, a 10 MB return is a clean error, a GIL-holding hang is TLE within one per-test budget. | Done |
| 52 | P5-10 | P5 | Coach trust: untrusted-input rule, override bound, live smoke | (1) `system.md` never tells the model that the statement, code, judge output and prior feedback are data: a comment such as `# score everything 4` can flip `mastered` once Solved. Bounded - own key, own machine, the editorial is on disk - but the ladder is a product rule. Add a paragraph naming those sections as untrusted, require every score below 4 to be justified in `feedbackMarkdown`, and cut `v2` per the bump rule in `docs/COACH_PROMPTS.md`, since it changes the advice. (2) Manual override can set `mastered` with no submission, which unlocks the editorial and the `solution` rung; adopt D25 - an override reaches Solved at most - and test it. (3) Coach markdown is sanitised, but `rehype-sanitize` allows any `img src`, so an injected reply can make the browser fetch an arbitrary URL; restrict `img` in coach content to the assets route. (4) The one thing M2 could not prove: add `COACH_LIVE_TESTS=1` gating a small live suite (one feedback turn, one chat turn, one abort, per vendor) that CI never runs and a developer with a key runs before a prompt bump, plus a fixture set of five code states per pilot with the expected hint rung, so a prompt change can be scored from one command. `JudgeError` messages forwarded raw to the client and the missing `nosniff` header on hijacked SSE responses go here too. | Done |
| 53 | P6-0 | P6 | Seed catalogue QA | Audit 2026-09-17 on the 20 seed problems, before the format is copied 180 times. (1) `pair-sum-index`, `shift-right-in-place` and `balance-point` state constraints 25–100× larger than any test reaches (`pair-sum-index` says 10^5 and generates 1000 because `_pair_count` is quadratic), so the quadratic solution the editorial says will time out passes - the defect the P2-10 note says was fixed. Adopt D21: generate at the stated maximum with an O(n) uniqueness check, or lower the constraint; add a validator warning when the largest hidden input is under half of a `<= N` bound parsed from the Constraints section. (2) Rung 4 of the hints is the full solution in about half the catalogue (`frequency-order` rung 4 is the reference's one line), against `PROBLEM_FORMAT.md` and the coach's ladder; rewrite rung 4 as the shape of the answer without code or exact conditions, and add the check to the AUTHORING checklist. (3) Every statement's `Input:` / `Output:` lines are joined by a soft newline, which markdown renders as one paragraph; separate them in all 20 statements and the scaffold, and reject the pattern in the validator. (4) Validator gaps: samples duplicated into `hidden[]` (`pair-sum-index` runs its three samples twice on Submit), hints `min(1)` while the docs require 4, no editorial section check, no check that `ops[].method` exists in the starters, no `tests.json` size cap, a placeholder rule that matches only `TODO`; `PROBLEM_FORMAT.md` section 12 lists ten static rules and omits the four the code adds. (5) Two `__pycache__/*.pyc` files are tracked under `problems/`; untrack them and set `sys.dont_write_bytecode` in `run_generator.py`. (6) `pair-sum-index/editorial.md` claims `target - values[i]` stays inside `int` when it can overflow; `tag-index` at rating 6 beside the structurally identical `min-value-stack` at 4; `even-odd-partition` tagged in-place with `space: O(n)`; the scaffold emits `int` returning 0 for `mutatedArgs`, where the method must be `void`; `writeHiddenTests` reflows the hand-formatted `meta.json`. | Done |
| 54 | P4-13 | P4 | Workspace polish from the audit | (1) Every keystroke re-renders the workspace and `Markdown` is not memoised, so the statement, hints, editorial and every coach turn are re-parsed through remark, rehype and lowlight per keystroke; `memo(Markdown)` on content, `memo` on `StatementPanel` and `CoachPanel`, and stop recreating the `coach` element per render. (2) Settings number fields commit only in-range values into a controlled input, so typing "1" on the way to "12" snaps back, and each keystroke is an undebounced PUT; local string state, commit on blur or Enter. (3) The list's search box is seeded from the URL once, so Back and Forward through `?q=` leave stale text. (4) Two rapid settings PUTs can resolve out of order and repaint the older theme; ignore a response that is not the latest mutation. (5) `ConfirmDialog` uses `bg-black/40` - a raw colour, checklist item 1 - so add an `--overlay-scrim` token; `aria-label` on a bare `span`; `h4` under no `h3` in `ResultsPanel`; operations results render every op row uncapped while values are capped at 6 KB. (6) The axe suite audits four idle screens and the Coach tab only: add a pass after a Run (results, diff, compile-error list) and with the reset dialog open, and give a11y its own Playwright project with `workers: 1` - `mode: 'default'` does not opt out of `fullyParallel`, so the theme race the P4-10 note describes is still possible locally. | Done |
| 55 | P6-1 | P6 | Curriculum map | `docs/CURRICULUM.md`: for each of the 14 topics, the patterns to cover and the planned problem list (title, tier, rating, pattern, mode), ordered as a path. This is the content backlog for P6-2..P6-6. Audit 2026-09-17: also fix the pattern vocabulary here - `patterns` is free text and the seed already has "hash map", "hash set" and "frequency map", "two pointers" and "two passes" - and make it a zod enum once listed; flag every problem whose target complexity beats the obvious approach as a trap problem needing a maximum-size test (D21); mark the ListNode, TreeNode and graph problems so P2-12 is proven before they are drafted; require `order` to be unique within a topic, since ties are broken by slug today. Budget from the seed: the pipeline is not the bottleneck, review is - plan 30–45 minutes per problem including a human read of hints and editorial. | Done |
| 56 | P2-14 | P2 | Pipeline at 200 problems | Measured 2026-09-17: `problems:validate` takes 34 s for 20 problems (1.7 s each, mostly the two reference runs), so at 200 it is six minutes on every push on two OSes; and in dev the catalogue re-reads and re-parses all nine files of every problem, `tests.json` included (200–400 KB each), on every `GET /api/problems` - 70–100 ms now, about a second at 200. (1) Split the loader: `loadMeta` for the list, full `loadProblem` for the workspace, hidden tests read on demand by the judge only; cache in dev with mtime invalidation. (2) Incremental validation per D23: on pull requests validate only the problems changed against the base ref, unless `apps/server/src/judge/**`, any `checker.ts` or the harnesses changed, in which case validate everything; full on `main` and nightly; run reference executions through the judge queue at 4 rather than serially. (3) `problems:gen --check` (regenerate to a temp file, fail on diff) in the full lane, pinned to one Python minor: an edited generator with a stale `tests.json` passes today, and `random.Random`'s sequence methods are not stable across minors - CI runs 3.12 and the catalogue was generated on 3.14. (4) `problems:schema --check` in the static job; `emit-schema.ts` says CI does this and it does not. (5) A Python 3.10 cell for the judge integration tests, since the harness promises 3.10 and nothing runs it below 3.12. | Done |
| 57 | P6-2 | P6 | Batch A: Arrays, HashMap, Sorting, Binary Search | ~45 problems total (extending the seed). Patterns: two pointers, sliding window, prefix sum, Kadane, intervals, frequency maps, grouping, complement lookup, custom sort, counting sort, boundary/rotated/answer binary search. Generators for every problem; all validated in CI. | Not started |
| 58 | P6-3 | P6 | Batch B: Linked List, Stack, Matrix | ~40 problems. Fast/slow pointers, in-place reversal, dummy head, merge; monotonic stack, parsing, min-stack; spiral, rotation, grid BFS/DFS, islands. | Not started |
| 59 | P6-4 | P6 | Batch C: Binary Tree, Heap, Graph | ~45 problems. Traversals, BST ops, LCA, construction, serialisation; top-K, two heaps, merge-K, streams (operations mode); BFS/DFS, topological sort, union-find, Dijkstra, bipartite. | Not started |
| 60 | P6-5 | P6 | Batch D: Backtracking, DP, Bit Manipulation | ~45 problems. Subsets/permutations/combinations (unordered comparators), N-Queens, word search; 1D/2D DP, knapsack, LCS/LIS, edit distance, DP on grids; XOR tricks, bit counting, bitmask DP. | Not started |
| 61 | P6-6 | P6 | Batch E: Advanced data structures | ~25 problems, mostly operations mode. Trie, segment tree, Fenwick, union-find design, LRU/LFU, monotonic deque, design-style problems. | Not started |
| 62 | P6-7 | P6 | Content QA | Regenerate hidden tests with larger seeds and confirm both references agree (differential testing); LLM-assisted review pass for ambiguity and missing constraints; difficulty calibration by solving time; fix findings. | Not started |
| 63 | P7-1 | P7 | Progressive hints | Hints tab reveals one rung of `hints.json` at a time; reveals recorded and passed to the coach, which treats the static ladder as canonical and adapts it to the user's code. Static hints are the no-key fallback. Audit 2026-09-17: the one-rung-at-a-time reveal already exists client-side (`StatementPanel.tsx`); what remains is persisting reveals, sending `revealedHints` (always 0 today), and the tab-state fix in P4-12. | Not started |
| 64 | P7-2 | P7 | Editorial unlock | Editorial tab unlocks after Solved or via explicit "reveal solution" (recorded). Shows approach, complexity, pitfalls and reference code in both languages with a diff-against-my-code view. Audit 2026-09-17: the lock-until-Solved half exists (P4-6); the recorded reveal and the diff view remain. | Not started |
| 65 | P7-3 | P7 | Submission history | List of submissions per problem with verdict, language, time, problem version; open any, diff against current editor, restore into editor. Audit 2026-09-17: the Submissions tab exists (P4-6); open, diff and restore remain, plus the cursor pagination P7-9 adds. | Not started |
| 66 | P7-4 | P7 | Notes | Per-problem markdown notes with autosave; searchable from the list. | Not started |
| 67 | P7-5 | P7 | Progress dashboard and skills report | `/progress`: solved/mastered per topic, per tier, streak calendar, recent activity, weakest topics (from coach rubric scores). Export a shareable skills report as JSON, markdown and a self-contained static HTML page. | Not started |
| 68 | P7-6 | P7 | Interview mode | Optional timer (stopwatch or countdown), hides hints/editorial while running, records time-to-solve on the submission; coach adds an "interview communication" section: how to explain the approach and complexity out loud. | Not started |
| 69 | P7-7 | P7 | Navigation aids | Command palette (`Ctrl+K`), bookmarks, random unsolved problem, "next recommended" (lowest rating unsolved in weakest topic), related problems from `meta.related`. | Not started |
| 70 | P7-8 | P7 | Spaced repetition review queue | Solved problems resurface after 3/7/21 days for a re-solve without hints; Mastered extends intervals. Queue visible on dashboard. | Not started |
| 71 | P7-9 | P7 | Problem version drift | `problem_version` is stamped on every submission and read by nothing. When a problem's tests change (P6-7 regenerates with larger seeds, P6-0 fixes constraints), a Solved earned against the old tests keeps its status with no record that the bar moved. Show "solved on v1; tests changed in v2" on the row and in the workspace header, offer Re-verify (re-submit the last accepted code through the current tests, recorded as a normal submission), and never demote automatically - D11's ratchet stands, the user decides. Dashboard (P7-5) counts drifted solves separately. Add `before` cursor pagination to the submissions list while in here; today it is a bare `limit`. | Not started |
| 72 | P8-1 | P8 | Full E2E coverage | Playwright suites for filters, workspace, every verdict type incl. operations and mutated-args problems, coach panel (mocked), settings, dashboard, keyboard-only flow. Flake budget tracked; retries only in CI. | Not started |
| 73 | P8-2 | P8 | Performance | Measure list with 500 problems and virtualise only if needed; Monaco code-split; judge queue under load; optional warm `javac` compile server if Java Run latency exceeds 1.5 s; server cold start under 2 s; Lighthouse performance and a11y ≥ 90 on both themes. Measured 2026-09-17 on the owner's machine: a Java Run (samples plus four hidden tests) is about 750 ms end to end and a Python Run about 160 ms, so the warm `javac` server is not needed; a compiled-class cache keyed by code hash is the cheaper option if Java ever crosses 1.5 s. Production ships source maps (16 MB of `.map` files); acceptable for a local tool, keep it deliberate. | Not started |
| 74 | P8-3 | P8 | Packaging and first-run | `npm start` builds once and serves on 127.0.0.1; first-run doctor checks `python`/`java`/`javac` on PATH and versions (Python ≥ 3.10, Java ≥ 21) with install guidance; short welcome explaining Run, Submit and AI Help; `data/` location configurable; backup/restore of the DB. Audit 2026-09-17: on Windows the `python` on PATH is often the Microsoft Store alias stub, which opens the Store instead of running; the doctor must detect it (`python -c` exit code and the `WindowsApps` path) and point at `DEVPROMAX_PYTHON`. Also verify `npm ci --omit=dev` starts, which P3-6 makes possible. | Not started |
| 75 | P8-4 | P8 | Documentation | README (quick start, screenshots), `docs/ARCHITECTURE.md`, `docs/PROBLEM_FORMAT.md`, `docs/AUTHORING.md`, `docs/DESIGN.md`, `docs/COACH_PROMPTS.md`, CHANGELOG. Audit 2026-09-17: README and CLAUDE.md corrected in the audit commit (the README said the coach reviews every run; both said M2 was next). Remaining: CHANGELOG, screenshots, and moving the revision history out of this file into it once it outgrows section 7. | Not started |
| 76 | P8-5 | P8 | Retire `temp/` | Owner wants `temp/` kept until the app is built out. Revisit at M5; delete only with explicit confirmation (history keeps it). | Blocked |
| 77 | P9-1 | P9 | Mock interview mode | LLM acts as interviewer: picks 2 problems for 45 min, asks for verbal approach before coding, probes complexity, produces a written debrief. | Not started |
| 78 | P9-2 | P9 | Docker executor | Optional sandboxed executor when Docker is available; same executor interface. | Not started |
| 79 | P9-3 | P9 | More languages | C++, JavaScript/TypeScript, Go via new harnesses; content gets references per language incrementally. | Not started |
| 80 | P9-4 | P9 | OpenAI-compatible provider | Adapter for OpenAI-compatible endpoints (`baseUrl`), which also covers Ollama/LM Studio for fully offline coaching. | Not started |
| 81 | P9-5 | P9 | Format on save | Optional `black` and `google-java-format` integration via subprocess, detected at first run. | Not started |

---

## 6. Milestones

| Milestone | Includes | Exit criteria |
| --- | --- | --- |
| M0 Walking skeleton | P0-5 … P4-1 | **Reached 2026-09-17.** All three pilot problems (function, in-place, design) solved in Python and Java from the browser, checked by `e2e/m0.spec.ts`; judge integration tests green on both OSes. |
| M1 Vertical slice | P4-2 … P4-10 with the 20 seed problems | **Reached 2026-09-17.** `e2e/flows.spec.ts` walks the whole slice - filter, open, run, submit, Solved in the list, reload - and again by keyboard alone; `e2e/a11y.spec.ts` audits four screens in two themes. Twenty-one E2E tests green locally on Windows; the per-PR Ubuntu lane runs the same suite. |
| M2 Coach | P5-1 … P5-7 | **Reached 2026-09-17.** AI Help streams rubric feedback into the Coach panel, the local pre-check refuses an untouched starter without spending anything, and an accepted solution the coach scores 4 across the rubric reaches Mastered. 214 coach tests, none of which touch the network; `e2e/coach.spec.ts` walks the two paths that need no vendor through a real event stream. The streaming half against a live vendor is the one thing no test can prove - it needs a key. |
| M2.1 Audit fixes | P5-8 … P4-13 | **Reached 2026-09-18.** Every confirmed defect from the 2026-09-17 audit is closed, each with a test named in its row: the key is set from Settings and never reaches a judge child; Stop cancels the vendor request and chat is under the cap; a draft survives switching language twice and navigating away mid-debounce; `npm start` serves the app and Ctrl+C closes it; `Optional[ListNode]` decodes; output, results and hangs are bounded; the coach treats its context as data and cannot be handed Mastered by an override; the three under-tested constraints are true and rung 4 leaks nothing; and the workspace stops re-parsing its markdown on every keystroke. 1087 unit, 92 integration, 39 end-to-end, and the catalogue validates. Three of the fixes were found by their own tests rather than by the audit - the abort listener on the wrong object, the connection test calling any 200 a success, and a scrollable pane that could not hold focus. |
| M3 Full catalogue | P6-0 … P6-7 | ~200 validated problems across all 14 topics and three tiers, every one with a generator. |
| M4 Learning loop | P7-1 … P7-9 | Hints, editorial, history, notes, dashboard with shareable skills report, timer, review queue. |
| M5 Release 1.0 | P8-1 … P8-5 | Packaged `npm start`, docs complete, `temp/` retired. |

---

## 7. Revision history

- 2026-09-16 initial plan
- 2026-09-16 scope audit applied (judge test modes, mutation capture, batch execution, local-server hardening, statement renderer, authoring pipeline, walking-skeleton milestone, monorepo collapsed, `node:sqlite`, several cuts)
- 2026-09-17 the coach routes move from P3-1 to P5-1/P5-3, where the provider's streaming half is built; the API gains the manual-override and draft-delete routes the UI needs (D11 override, reset-to-starter).
- 2026-09-17 P0-8: design tokens and principles landed; the contrast rule in `docs/DESIGN.md` is enforced by a test that reads the token file, and `danger` split into a status fill and a button fill because one red cannot carry white text and still read on a near-black page.
- 2026-09-17 P2-9: the authoring pipeline cross-checks the two references at generation time (differential testing, which P6-7 extends rather than introduces), and the validator now rejects leftover scaffold placeholders.
- 2026-09-17 P2-10 seeded the catalogue to 20 problems; stated constraints are now the sizes the tests actually reach, after a first pass produced a 13.7 MB catalogue and two binary-search problems that claimed a linear scan would time out when it would not.
- 2026-09-17 M0 reached: P4-1 solves every pilot in both languages from the browser, and the API now accepts a bodyless POST that declares JSON, which Fastify rejects by default.
- 2026-09-17 P4-2..P4-7: the app shell, the statement renderer, the list with filters, the real workspace and the results UI. Three deviations worth recording: the workspace ships without the Notes and Coach tabs the P4-6 row named, because neither has anything behind it yet (P7-4, P5-1); `/settings` was built now rather than left a stub, because P4-6 reads editor preferences that had to be editable somewhere; and the RTL tests P4-9 lists shipped with the screens, per CLAUDE.md, leaving P4-9 the Playwright golden path. `docs/DESIGN.md` gains a rule about `calc(var(--spacing) * n)` after a stylesheet full of `var(--spacing-3)` silently dropped every margin in the statement panel.
- 2026-09-17 P4-8: the accepted-submit confirmation is the status mark in the workspace header, driven by the refetched problem and announced through a `role="status"` region - DESIGN.md section 2 rules out a celebratory toast, and a flag set by this session's own submit could not tell a user that yesterday's solve is still solved.
- 2026-09-17 P4-10: the axe suite is the first thing to run the app in both themes on purpose, and it found three real defects plus two of its own making - the theme is a *server* setting, so parallel workers repainted each other's pages mid-audit, and a settings write racing an in-flight settings read could bounce a theme choice back (`useUpdateSettings` now cancels the read). The skeleton fill is `border` rather than `surface-sunken`, because in dark `bg` and `surface-sunken` are the same colour and the placeholder was invisible on the one screen nobody had loaded slowly.
- 2026-09-17 M1 reached. P4-9 added `DEVPROMAX_DB`, which P8-3 would have added later anyway: the end-to-end suite submits real solutions through the real judge, and until now it wrote every one of them into `data/devpromax.db` - the developer's own history. The golden path counts submissions rather than reading the status word, because a suite that shares a database with a human cannot assume it starts from nothing.
- 2026-09-17 P5-1: the provider seam yields raw JSON text rather than parsed objects, because structured output is not parseable until its last chunk and the panel is meant to show prose being written. `partialJson.ts` reads one string field out of a half-arrived document so a single request feeds both the streaming panel and the rubric card; the alternative was two requests that could disagree with each other. Transport is per vendor (see D12). The Anthropic key check moved onto the SDK with it, and the existing connection tests passed unchanged - the SDK sends the same headers the hand-written `fetch` did.
- 2026-09-17 P5-2: the system prompt is a versioned `.md` file read at load, not a string in code, because it is both the thing most likely to be edited as prose and the thing `cache_control` points at. `copy-assets.mjs` gained the directory and a filter, since `coach/prompts` is the first asset folder that also contains a module tsc compiles. The context budget counts characters, not tokens: a tokeniser per vendor is precision bought for a limit that only has to be conservative. The code is the one section never truncated - a review of half a function is confidently wrong about what is missing, and the user cannot tell.
- 2026-09-17 P5-3: the coach answers as an event stream, which forces a split worth recording - once the first frame is written the status code is already sent, so a provider that fails on the third token arrives as an `error` event and only pre-stream failures get a status. A local refusal is a `skipped` event on a 200: nothing went wrong, the request simply was not worth making, and a 4xx would read as "you did something invalid". `StreamOptions.schema` became optional so chat can ask for prose; a bare JSON string would have arrived quoted and escaped. Auditing the new tab found a heading jump from `h1` to `h3` in the testcase panel that Hints and Submissions had carried since P4-6 - Description's markdown supplied the missing `h2`, so the only tabs that exposed it were the ones nothing audited. Fixed, and `e2e/a11y.spec.ts` now opens the Coach tab in both themes.
- 2026-09-17 P5-4: mastery needed no new status-engine work - P3-3 already refused `coach_mastered` below Solved - so the task was the two gates above it and the nudge. The second gate is worth naming: `mastered` and the five scores can disagree, and the scores win, because a model that sets a boolean while scoring a 2 somewhere would otherwise decide what Mastered means.
- 2026-09-17 P5-5: the first migration since `001`, and it applied to a live database with a hundred submissions in it without loss - `ALTER TABLE ADD COLUMN` is additive, and turns written before it simply carry no delta rather than being dropped from the memory. A diff rather than the previous file, because the volatile half of the prompt is exactly the part `cache_control` cannot cover, so every token of it is paid for on every request.
- 2026-09-17 P5-6: two findings worth recording. Pino's `*` matches exactly one level, so the redact list had covered `{coach: {apiKey}}` and silently not `{settings: {coach: {apiKey}}}` - which is the shape the API actually passes around; `logger.test.ts` now proves each depth against a real logger rather than trusting the list by reading it. And the judge-only fallback lost a heuristic on the way in: "every failing input is empty or minimal" was dropped rather than tuned, because whether `target = 0` is minimal depends on the problem, and a local guess that reads like coaching is worse than saying less.
- 2026-09-17 M2 reached. P5-7 found two things the per-task tests could not: the provider contract suite proves the two vendors are interchangeable rather than merely each tested, and the first e2e coach run that was repeated caught a test polluting its own next run - typing autosaves a draft, so "the untouched starter" stopped being untouched the second time. `resetDraft` and a second problem fix it, and the suite is now green twice in a row on the same database.
- 2026-09-17 audit of the codebase and this plan, at M2. Every lane was green (typecheck, lint, 980 unit, 67 integration, 28 e2e, validator), so the findings are gaps rather than breaks. Thirteen tasks added, P5-8 to P7-9, each carrying its evidence in the row; the block before P6-1 is milestone M2.1. Decisions D19–D25 record the answers to the questions the audit raised. Corrections to rows already Done are appended to those rows and dated, so the record of what shipped stays intact. Two things worth naming: the coach's settings UI was deferred by P4-2 to P5-3 and by P5-3 to P5-6, and neither built it, so the feature marked M2 can only be configured from an environment variable; and the judge child inherits `process.env`, which puts `COACH_API_KEY` one `print` away from the results panel. The revision history moved here from the header, where it had become one six-kilobyte paragraph.
- 2026-09-17 P5-8: the coach is configurable from the UI, which needed one new seam - `ProviderOptions.baseUrl`, with `DEVPROMAX_COACH_BASE_URL` as its env override - because "Test connection with a fake key" is otherwise a test that sends something key-shaped to a vendor over the real internet. Pointed at the API's own `/api` prefix, the client-header rule answers 403 and the provider maps that to "rejected that API key", which is both the honest answer and a deterministic one. Settings moved into `screens/settings/` and its field rows into `fields.tsx`: the key needs an explicit Save (a PUT per keystroke would write eight prefixes of a secret to disk) and the spend cap needs local text state (an empty field means "no cap" and is also what the first keystroke of a number looks like), so the no-Save-button rule now has two documented exceptions rather than one undocumented one.
- 2026-09-17 P2-11: the child environment is an allow-list in `childEnv.ts`, not a filtered `process.env`, and the direction is the point - a deny-list is wrong the day someone adds a variable and forgets it, and the thing forgotten is by definition the new secret. The list is matched case-insensitively, because `process.env` keeps the parent's casing and a literal `PATH` lookup finds nothing on a normal Windows shell. The Java harness renamed its internals to `DevProMax*` and is now compiled *before* the user's file, since javac reports a duplicate class at its later definition - so `class ListNode` lands on the user's own line with a message that says the judge already defines it, where it used to be reported as "the judge's harness failed to compile". `dispose` swallows and logs, because it runs in a `finally` and a locked `.class` file was turning a passing submission into a 500.
- 2026-09-17 P5-9: the cancel path was written twice before it worked. `request.raw.on('close')` - what the audit row proposed and what looks right - never fires for a POST whose body has already been read, so the listener heard nothing; it is `reply.raw` that closes when the browser hangs up. The route test that listens on a real socket is the only thing that could have caught it, and did. The rest: chat is under the cap and the cost accounting feedback always had, a conversation sends one feedback context rather than all of them (D20), usage is reported from a `finally` so a turn that failed halfway is not recorded as free, a cancelled turn's cost lands on the context row it opened with, cache writes are charged at 1.25x (the claim that caching only lowers a bill is true from the second turn and wrong for the first, which writes the whole system prompt), `max_tokens` is 32k at medium effort because the old 8k was shared with adaptive thinking and a long think truncated the JSON into a "retryable" failure that cost the same again, and Gemini's adapter merges same-role turns so one failed turn cannot 400 every later one.
- 2026-09-17 P4-11: the save had always worked; the cache had never been told, so the editor was re-seeded from the problem the page loaded with. Writing the confirmed draft through with `setQueryData` fixes the ten-minute loss, and the end-to-end test then found a second one the audit had not: switching language and straight back re-seeds before the flush's PUT has answered, which shows the starter over code typed a second ago. A per-scope buffer recorded on the way out covers that window. The autosave is three effects now rather than one - the debounce's cleanup must not flush, or every keystroke writes, and the flush's cleanup is the only one that means "we are leaving". `persisted` moves in `onSuccess`, because marking it saved before the PUT meant a failed save was neither retried nor noticed.
- 2026-09-17 P4-12: a turn now ends only its own stream (the shared `finally` was ending whichever was newest), a modal owns the keyboard while it is open, and the two panels holding something the user typed - the coach's question and the results selection - stay mounted, which also puts the hint count in `Workspace` where P7-1 needs it. `revealedHints` therefore stops being 0 on every request. Two error boundaries, one in the shell and one around the lazy editor, because a chunk that has gone missing after a server restart was a white page. Two lessons for the tests: a fake provider that ignores the abort signal makes the turn-ownership test pass against the unfixed code, and the drafts end-to-end test was inheriting `lastLanguage` from whichever test ran before it - a *setting* is shared state, so the test now chooses the language it means to test.
- 2026-09-17 P3-6: `npm start` now builds and then serves both halves from one process on one port (D24), and the new `start.ts` exists for a reason worth recording - `NODE_ENV` has to be set before `logger.ts` is imported, or a production install loads `pino-pretty` from devDependencies, and no shell-level way of doing that works the same in PowerShell, cmd and sh. The first version of that launcher relied on `index.ts`'s entry-point check, which compares `argv[1]` against itself: it built everything, started nothing and exited 0, and the end-to-end spec is what caught it. The SPA fallback lives in the *existing* not-found handler rather than a second one, because Fastify allows one per instance. Also: a non-numeric `DEVPROMAX_PORT` and a port in use now say what to do, and Ctrl+C closes the database and kills judge children - which needed a registry of live children, since Node kills this process and not the `java` it started.
- 2026-09-17 P2-12: Python annotations are rendered recursively now (`render_annotation`) rather than named with `__name__`, which on this Python turned `Optional[ListNode]` into `"Union"`; the decoder works on the rendered text because half of all annotations arrive as strings anyway - `from __future__ import annotations` and quoted forward references both give text and nothing else. Node arguments and returns are covered by integration tests in both languages before a single catalogue problem needs them, which is the point of doing this before P6-3. Two findings while wiring the operations-mode error: `List.copyOf` throws on the nulls a void method contributes, so every failing operation was arriving as a NullPointerException from inside the harness; and the judge dropped the partial returns even once the harness reported them, so surfacing them as `actual` was a change on both sides. One audit item needed no work - the schema already refines `mode: operations` to `expect: 'return'`, checked rather than assumed.
- 2026-09-17 P2-13: both harnesses now drop output as it arrives rather than capping a buffer that already grew, both refuse a result line past 2 MB, and the judge watches the results file so an uninterruptible call is killed on a per-test budget instead of the batch's whole wall clock. The timing bound is asserted in `process.test.ts` with a fake progress function, not in the integration suite - a wall-clock assertion over real interpreters measures the isolation fallback as much as the fix, and the honest split is "the bound" in a unit test and "a GIL-holding hang is a TLE" in an integration one. `fsync` per record is gone: the reader is a process that starts after this one exits.
- 2026-09-17 P5-10: prompt `v2` states the rule the app was relying on without saying - the context is material to review, not instructions to follow - and requires every score below 4 to be justified in the prose. The override clamps to Solved rather than refusing the request (D25), because the highest status the user can honestly claim is still theirs to claim, and `masteredAt` is cleared with it since the timestamp records a decision the coach no longer stands behind. `Markdown` gained a `trust` prop: `rehype-sanitize` allows any `img src`, so an injected image in a coach answer was a beacon that fired the moment the panel painted - no click required. The live suite is the piece M2 could not have: gated on `COACH_LIVE_TESTS=1` plus a key, it does one real turn and one real cancellation per vendor and then scores five code states against the ladder, asserting direction rather than wording and only in the generous direction.
- 2026-09-18 P6-0: seven new validator rules, and every one of them was a defect the seed catalogue already had - which is the argument for checking them mechanically. Two were harder than they looked. The stated-constraint check (D21) has to tell a *size* bound from a *value range*: a first version read `-10^9 <= values[i] <= 10^9` as a claim about how much input there is and flagged every problem in the catalogue. The no-code-in-hints check has to look for structure rather than keywords: a first version matched `for ... :` and rejected the sentence "two things are being asked for: how often each value occurs". The duplicate-sample rule is enforced in the pipeline as well as the validator, because the generator is where the duplicate came from. pair-sum-index now generates at 10^5 with a constructed one-pair case rather than a quadratic rejection loop, which is what makes its editorial's "the quadratic times out" true; shift-right-in-place's claim was lowered to 10^4 instead, since its trap is space and no size proves that. Along the way the coach's connection test turned out to call any 200 a success - a stand-in vendor that answered with an HTML page read as "connected" - so an unreadable model list is now a failure, which is P5-10's row but this task's find.
- 2026-09-18 P4-13, and M2.1 with it. The memoisation needed three changes rather than one: `memo` on the two panels does nothing while the caller builds the coach element inline, and a callback closing over `useCoach`'s wrapper changes identity every render even though every function inside it is stable. The a11y pass added here found a real `serious` violation on its first run - the results detail pane scrolls and could not hold focus - and the fix needed the `jsx-a11y` rule relaxed for grouping roles, because axe and that rule disagree by default and both are right. Three of the four end-to-end failures chased today were the same thing wearing different hats: the language is a *setting*, shared by every spec and every earlier run, so a test that does not choose one is testing whatever the last test chose.
- 2026-09-18 P6-1: `docs/CURRICULUM.md` plans 175 problems rather than the 200 the original row named, and the cut is the point - a problem that teaches nothing the one before it did not is worse than no problem, and the tables were trimmed twice on that rule. `patterns` is a closed enum now (`packages/shared/src/patterns.ts`), which immediately found four spelling variants in twenty problems, and `order` must be unique within a topic, which found eight seeded problems whose numbering disagreed with the path they belong to. The scaffold had to stop writing `TODO` into `patterns`: with an enum, a placeholder there makes `meta.json` unparseable and hides every other thing the validator would have said, so it guesses `one pass` and the CLI says so in step 0.
- 2026-09-18 P2-14: measured before and after on the seed - the list path went from 122 ms to 5 ms cold and 2 ms warm, and `problems:validate` from 34 s to 15 s. Three changes did it: the list reads `meta.json` only, nothing the API serves parses the hidden tests (the judge asks for them by name), and the cache is keyed on modification time in every environment rather than switched on by `NODE_ENV`. The hidden pool is dropped *before* zod rather than after, which is where the cost actually was. `--changed` narrows the reference runs and never the static rules, because the cross-problem checks are exactly the ones a diff cannot see. `problems:gen --check` closes the gap where an edited generator with a stale `tests.json` passed everything, and it refuses to run on the wrong Python minor rather than reporting a difference that means nothing - which is what `problems/GENERATED_WITH` is for.
