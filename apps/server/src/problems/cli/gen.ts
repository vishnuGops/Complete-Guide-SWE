#!/usr/bin/env tsx
/**
 * `npm run problems:gen <slug>` — regenerates hidden tests from generator.py.
 * Implemented in ROADMAP task P2-9 (authoring pipeline).
 */
console.error('problems:gen is not implemented yet — see ROADMAP task P2-9.');
process.exit(1);
