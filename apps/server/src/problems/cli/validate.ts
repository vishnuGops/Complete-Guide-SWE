#!/usr/bin/env tsx
/**
 * `npm run problems:validate [--static] [slug]`
 * Implemented in ROADMAP task P1-2 (static) and P2-7 (reference execution).
 */
console.error('problems:validate is not implemented yet — see ROADMAP task P1-2.');
process.exit(1);
