#!/usr/bin/env tsx
/**
 * `npm run problems:new <topic> <slug>` — scaffolds a problem package.
 * Implemented in ROADMAP task P2-9 (authoring pipeline).
 */
console.error('problems:new is not implemented yet — see ROADMAP task P2-9.');
process.exit(1);
