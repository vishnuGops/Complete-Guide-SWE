{
  "hints": [
    "With `n` counts, how large can the answer possibly be? What does that tell you about which values are worth looking at?",
    "The answer is between 1 and `n + 1`, because `n` values cannot cover `1 .. n + 1`. Everything outside that range is junk.",
    "If you could put value `v` at position `v - 1` for every `v` in range, the answer would be the first position holding the wrong value. A hash set of the in-range values does the same job in `O(n)` space.",
    "Two passes. The first sends each in-range value to the position it belongs at by swapping, and keeps working the same position until what sits there cannot be placed; the second finds the first position that never got its value. Mind the value whose home already holds a copy of it, or the swapping never stops."
  ]
}
